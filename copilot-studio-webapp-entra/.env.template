# ==========================================================================
#  Custom Copilot Studio POC — Backend Configuration
# ==========================================================================

# ── Server ────────────────────────────────────────────────────────────────
PORT=3978
ALLOWED_ORIGIN=http://localhost:3000

# ── Entra ID App Registration ────────────────────────────────────────────
# Create in Azure Portal → Entra ID → App Registrations
# Platform: **Web** (not SPA), Redirect URI: http://localhost:3000
# API Permissions → Power Platform API → CopilotStudio.Copilots.Invoke
# Certificates & secrets → create a Client Secret
TENANT_ID=
CLIENT_ID=
CLIENT_SECRET=
REDIRECT_URI=http://localhost:3000

# ── Copilot Studio Connection String ─────────────────────────────────────
# Get from Copilot Studio → Channels → Web app → "Microsoft 365 Agents SDK"
COPILOT_CONNECTION_STRING=
